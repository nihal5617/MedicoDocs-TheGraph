{
  "name": "medicodocs",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ medicodocs",
    "create-local": "graph create --node http://localhost:8020/ medicodocs",
    "remove-local": "graph remove --node http://localhost:8020/ medicodocs",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 medicodocs",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.46.1",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
